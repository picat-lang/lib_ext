% to run: picat picasp.pi examples/asp_embedded_in_picat.pi %sat
% or:   picat picasp.pi cp examples/asp_embedded_in_picat.pi %cp

import aspic_runtime.
main=>println("testing ASP embedded in Picat!"),asp
#const n=8.
{a(X):X=1..n}=1.
end,
Y=a(3),Y#=0, % a constraint enforced in Picat
aspic_solve_sub(ASPIC_OPT_1),
println([a(X):X in 1..8]),fail;true.

